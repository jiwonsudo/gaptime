# gaptime (서비스명: 에브리프리타임)

여러 명이 각자 자기 기기에서 에타 시간표 스크린샷을 업로드하면, When2meet처럼 **방(room) 링크 하나로 실시간 취합**되어 가능 인원 수에 따른 히트맵을 보여주는 웹 서비스.

서비스명 **"에브리프리타임"**은 에브리타임("에브리"+"타임") 사이에 "프리(free)"를 끼워넣은 이름으로, "모두의 빈 시간"이라는 기능을 그대로 담고 있다. 레포/코드베이스 이름은 계속 `gaptime`을 사용하고, UI에 노출되는 서비스명만 "에브리프리타임"을 쓴다.

## 문제 정의

팀플/스터디 시간 잡을 때 실제로 벌어지는 일: 단톡방에 "다들 시간표 캡처해서 올려줘" → 한 명이 스샷 여러 장 눈으로 대조 → 시간 잡음. 번거롭고 실수도 잦음.

**When2meet처럼 가야 하는 이유**: 시간표는 각자 다른 폰에 있다. 한 사람이 이미지를 전부 모아서 한 세션에서 처리하는 방식(초기 구상)은 결국 "누군가에게 스샷을 다 보내야 한다"는 원래 문제를 그대로 안고 간다. 방 링크를 공유해서 **각자 자기 기기에서 업로드**하고, 서버가 결과만 취합해서 실시간으로 보여줘야 진짜 문제가 풀린다.

**핵심 설계 통찰 (유지)**: 과목명을 읽을 필요가 없다. 필요한 건 "이 칸이 비어있냐/차있냐" 뿐이다. 에타 시간표는 빈 시간 = 무채색, 수업 있는 시간 = 유채색으로 렌더링되므로, 텍스트 OCR 대신 픽셀의 **채도(saturation)**만 판별하면 된다. 이 접근은 과목별 색상, 라이트/다크모드, 폰 OS 차이에 전부 자동으로 강건하다.

반대로 자동으로 해결이 안 되는 변수는 "그리드가 이미지의 어느 픽셀 범위인가"다. 이건 완전 자동화 대신 **사용자가 몇 초간 그리드 경계를 직접 맞추는 반자동 캘리브레이션**으로 우회한다.

## 설계 원칙

1. **이미지는 각자의 브라우저를 벗어나지 않는다.** 캘리브레이션과 채도 분석은 100% 클라이언트에서 처리한다. 서버로는 원본 이미지가 아니라 **처리 결과(요일×시간 occupancy 배열 + 이름)만** 전송된다. 원본 스크린샷보다 훨씬 가벼운 데이터만 오가면서, "서버가 내 시간표 이미지를 본 적이 없다"는 프라이버시 셀링포인트도 유지된다.
2. **반자동 캘리브레이션.** 완전 자동 그리드 인식을 목표로 하지 않는다. "자동 추정 + 사용자가 보정" 하이브리드로 안정성을 확보한다.
3. **MVP 먼저.** 아래 "MVP 범위"를 벗어나는 기능은 구현하지 않는다. 스트레치 골은 MVP가 실제로 동작한 뒤에 착수한다.
4. **회전 없다고 가정.** 스크린샷은 기울어지지 않은 것으로 가정하고, 4점 perspective transform 대신 좌상단/우하단 2점 bounding box로 시작한다.
5. **결과 화면은 When2meet 스타일 히트맵.** 전원 일치하는 시간만 이진법으로 보여주는 것보다, "5명 중 4명 가능한 시간"까지 명암 단계로 보여주는 게 실사용성이 훨씬 높다. 셀 hover 시 누가 가능한지 이름도 표시한다.
6. **방(room) 기반 협업.** 한 명이 방을 만들면 짧은 링크가 생성되고, 그 링크로 들어온 사람들이 각자 자기 시간표를 업로드한다. 결과 화면은 방에 있는 모든 사람이 실시간으로 함께 본다.

## 기술 스택

- Vite + React + TypeScript
- **shadcn/ui** (Tailwind CSS + Base UI 기반, 컴포넌트를 복사해서 소유하는 방식) — 버튼/입력폼/Dialog(방 생성)/Tooltip(hover 시 이름 표시) 등 주변 UI에 사용. 핵심 화면(히트맵 그리드, 캘리브레이션)은 Canvas로 직접 구현하므로 shadcn은 그 외 UI 껍데기 담당
- Canvas 2D API (클라이언트 이미지 처리 — 캘리브레이션, 채도 샘플링)
- **Supabase** — Postgres(방/제출 데이터 저장) + Realtime(다른 사람이 업로드하면 히트맵 즉시 갱신)
- 상태관리: React 기본 state (필요해지면 Zustand 도입 검토)
- 배포: Vercel 정적 호스팅 + Supabase 매니지드 백엔드
- 로그인/계정: 없음 (방 링크 자체가 접근 권한 — When2meet과 동일한 방식)

## 핵심 알고리즘 (클라이언트에서 처리, 방마다 동일)

1. **색상 판별**: RGB→HSV 변환 → saturation 값만 사용. threshold(초기값 15~20%, 실측 이미지로 튜닝 필요) 미만이면 "무채색=빈 시간".
2. **샘플링 위치**: 셀 가장자리는 격자선 때문에 오판 가능성이 있으므로, 셀 안쪽 60% 영역에서 5x5 포인트를 샘플링해 평균 채도로 판정한다.
3. **그리드 분할**: 캘리브레이션으로 확정된 bounding box를 요일 수(기본 5, 월~금) × 시작~종료 시각(1시간 단위 기본)으로 균등 분할한다.
4. **서버 전송**: 계산된 occupancy 배열(예: `[[true,false,...], ...]`)과 이름만 방 ID에 묶어 Supabase에 저장. 이미지 자체는 처리 즉시 폐기, 서버로 전송되지 않는다.
5. **결합**: 서버(또는 클라이언트가 방의 전체 제출 목록을 구독해 계산)에서 셀마다 "가능 인원 수"를 카운트해 히트맵 명암 단계로 매핑, 누가 가능한지 이름 목록도 함께 보관. 최대 30명 규모를 고려해 명암만이 아니라 정확한 "N/전체" 숫자를 항상 함께 노출한다. 분모(전체)는 `max(방의 expected_size, 실제 제출 인원 수)`로 계산 — 예상보다 적게 모이면 예상 인원 기준으로, 예상보다 많이 모이면 실제 제출 인원 기준으로 자동 전환된다.

## 방(Room) 플로우

1. **방 생성**: 요일 수 / 시작·종료 시각 / **예상 인원수(기본값 4, 슬라이더나 스테퍼로 최대 30까지 조절)**를 설정하고 방 생성 → 짧은 room ID/링크 발급 (예: `gaptime.app/r/ab3f9k`)
2. **링크 공유**: 단톡방에 링크만 붙여넣기
3. **각자 참여**: 링크 클릭 → 이름 입력 → 자기 시간표 스크린샷 업로드 → 캘리브레이션(2점 드래그) → 로컬에서 occupancy 계산 → 제출
4. **실시간 결과**: 방 페이지에서 모두가 같은 히트맵을 실시간으로 봄 (Supabase Realtime 구독, 누가 새로 제출하면 즉시 갱신)

## MVP 범위

- 방 생성 (요일 수 기본 5 고정, 시작/종료 시각 입력, **예상 인원수 기본 4명 · 최대 30명까지 슬라이더/스테퍼로 조절**) → 고유 room ID 발급
- 방 링크로 참여 → 이름 입력 → 이미지 업로드 → 2점 bounding box 캘리브레이션 → 채도 기반 occupancy 계산 (전부 클라이언트)
- occupancy 결과 + 이름을 Supabase에 저장 (원본 이미지 저장 안 함)
- 방 페이지: Supabase Realtime으로 실시간 히트맵 갱신, 셀에 "N/전체" 숫자 표기, hover 시 가능한 사람 이름 목록(많으면 상위 몇 명 + "외 N명"으로 축약)
- 방 최대 인원 30명 (참가자 식별은 이름 해시 기반 색상 점, 고정 팔레트 아님)
- 텍스트 export (예: "화 15~17시 (전원 가능), 목 13시~18시 (28/30명 가능)")
- 로그인/계정 없음, 방 링크가 곧 접근 권한

## 스트레치 골 (MVP 완료 후에만 착수)

- 왼쪽 시간 라벨 Tesseract.js OCR로 시작/종료 시각 자동 감지
- 회전 보정용 4점 perspective transform
- 토/일 컬럼 지원 옵션
- 카카오톡 공유 최적화 텍스트 포맷 버튼
- 방 자동 만료(예: 생성 후 30일 뒤 삭제) — When2meet도 오래된 방은 정리함
- 그리드 위치 자동 추정(엣지 검출)으로 캘리브레이션 스텝 단축
- 제출 취소/재제출 (자기 이름으로 다시 업로드해서 덮어쓰기)

## 명시적 비목표 (Non-goals)

Claude Code가 스코프를 임의로 넓히지 않도록 명시:

- 과목명/교수명 OCR 인식 — 필요 없음, 하지 않는다
- 완전 자동 그리드 검출 — MVP에서 시도하지 않는다
- **원본 이미지의 서버 저장/전송** — 절대 하지 않는다. 클라이언트에서 처리 후 즉시 폐기, occupancy 배열만 전송
- 사용자 계정/로그인 시스템 — 방 링크 기반으로 충분
- 회전된 스크린샷 대응 — 스트레치 골로 미룬다

## 디자인 컨셉

**핵심 아이디어**: 이 서비스의 히어로는 마케팅 카피가 아니라 **살아있는 히트맵 그리드 자체**다. 랜딩/방 생성 화면부터 예시 데이터로 실제 작동하는 그리드를 보여줘서 "무엇을 하는 서비스인지" 설명 없이 바로 체감하게 한다. 시간표(격자·칸·시간축)라는 소재를 장식이 아니라 UI 전체의 구조적 뼈대로 사용한다 — 로딩 상태도 셰이머 바가 아니라 빈 시간표 칸 모양으로, 빈 화면(방에 아직 아무도 제출 안 함)도 "비어있는 시간표"라는 톤으로 처리.

- **Color**
  - `#F3F5F0` — 배경 (차갑지 않은 페이퍼 화이트, 웜크림 계열 클리셰 피함)
  - `#1C231D` — 텍스트 잉크 (완전 검정 대신 그린 톤이 살짝 도는 다크)
  - `#3FA968` — 메인 액센트 = 히트맵에서 "전원 가능"을 나타내는 색 (공강/여유를 연상시키는 그린)
  - `#E4E2DC` — 히트맵에서 "아무도 안 됨" 상태 (중립 회색, 무채색 원칙과도 맞음)
  - `#FF6B4A` — CTA/링크용 보조 액센트 (방 만들기 버튼 등, 그린과 대비되는 따뜻한 톤)
  - 참가자 식별은 **고정 팔레트가 아니라 이름 문자열 해시 기반 HSL hue**로 생성 (최대 30명까지 대응해야 하므로 고정 색상 수로는 한계가 있음). 색은 hover 목록에서 이름 옆 작은 점으로만 쓰는 장식 요소이고, 실제 식별은 항상 텍스트 이름으로 한다 — 색이 겹쳐도 무방함

- **Type**
  - 본문/UI: Pretendard — 한글 가독성과 넓은 웨이트 지원
  - 헤딩/숫자(시간 라벨): 같은 Pretendard의 Bold~ExtraBold 웨이트를 크게 써서 별도 서체 도입 없이 위계 차이를 냄. 시간 숫자 라벨에 모노스페이스는 쓰지 않는다 (흔한 "데이터 라벨=모노스페이스" 클리셰 회피, tabular figure 옵션만 켜서 숫자 정렬만 맞춤)

- **Layout**
  - 방 페이지 진입 시 화면 상단 대부분을 히트맵 그리드가 차지 — 별도 히어로 문구/그라데이션 없음
  - 좌측 정렬 기본 (한글 본문 가독성), 그리드만 중앙 정렬로 시각적 무게중심 역할
  - 방 생성 폼은 그리드 위에 뜨는 모달(shadcn Dialog)이 아니라, 그리드 자체가 아직 안 채워진 상태(빈 칸)로 존재하고 그 옆에 설정 폼이 나란히 붙는 구조 — "이미 시간표가 거기 있고, 채워나간다"는 느낌
  ```
  ┌─────────────────────────────┬───────────┐
  │  월  화  수  목  금          │ 방 설정    │
  │ [빈 격자, 아직 아무도 없음]  │ 시작시각   │
  │                              │ 종료시각   │
  │                              │ [방 만들기]│
  └─────────────────────────────┴───────────┘
  ```

- **Principles**
  - 그리드가 곧 UI다 — 카드/보더radius로 콘텐츠를 나누는 SaaS 기본값 대신, 시간표 격자 자체가 레이아웃의 기준선이 된다
  - 숫자(가능 인원 수)는 굵기와 크기로 강조하되, 화살표(→)나 전각 대시, 올캡 라벨 같은 장식적 UI 문구는 쓰지 않는다
  - 모션은 새 제출이 실시간으로 들어와 셀 색이 바뀌는 순간에만 사용 (해당 셀 밝기 트랜지션) — 스크롤 등장 애니메이션 등은 넣지 않는다
  - **최대 30명까지 지원.** 인원이 많아지면 명암 단계만으로는 셀 간 미세한 차이(27/30 vs 28/30)가 구분되지 않으므로, 셀 안 또는 hover에 항상 "N/전체" 숫자를 함께 표기한다. hover 이름 목록은 30명 전부를 나열하지 않고 상위 몇 명만 보여준 뒤 "외 N명"으로 축약하고, 필요시 스크롤 가능하게 한다.



```
gaptime/
├── src/
│   ├── components/
│   │   ├── ImageUploader.tsx
│   │   ├── GridCalibrator.tsx      # 2점(→추후 4점) 드래그 캘리브레이션 UI
│   │   ├── TimeRangeForm.tsx       # 요일 수 / 시작·종료 시각 입력 (방 생성 시)
│   │   ├── RoomCreate.tsx          # 방 생성 UI
│   │   ├── RoomJoin.tsx            # 이름 입력 + 업로드 + 캘리브레이션 플로우
│   │   └── ResultGrid.tsx          # When2meet 스타일 히트맵 시각화 + hover + export
│   ├── lib/
│   │   ├── colorAnalysis.ts        # RGB→HSV 변환, saturation 판별
│   │   ├── gridSampler.ts          # bounding box → 셀 분할 → occupancy 배열
│   │   ├── overlap.ts              # 여러 occupancy 배열을 셀별 가능 인원 수로 결합
│   │   └── supabase.ts             # Supabase 클라이언트, 방/제출 CRUD + realtime 구독
│   ├── types.ts
│   ├── App.tsx                     # 라우팅: / (방 생성), /r/:roomId (참여+결과)
│   └── main.tsx
├── public/
├── index.html
├── package.json
├── vite.config.ts
└── README.md
```

## Supabase 스키마 (초안)

```sql
create table rooms (
  id text primary key,           -- 짧은 랜덤 ID
  day_count int default 5,
  start_hour int not null,
  end_hour int not null,
  expected_size int default 4,   -- 방 생성 시 설정한 예상 인원수 (기본 4, 최대 30)
  created_at timestamptz default now()
);

create table submissions (
  id uuid primary key default gen_random_uuid(),
  room_id text references rooms(id) on delete cascade,
  name text not null,
  occupancy jsonb not null,      -- boolean[][] (day x hour)
  created_at timestamptz default now()
);
```

## 작업 순서 (Claude Code 단위 작업)

1. Vite + React + TS 스캐폴딩, Supabase 프로젝트 연결
2. Supabase 테이블(rooms, submissions) 생성
3. 방 생성 플로우 (RoomCreate → rooms insert → room ID 발급/라우팅)
4. 이미지 업로드 + 캔버스 렌더링
5. 2점 드래그 캘리브레이션 UI
6. `colorAnalysis.ts` — RGB→HSV 변환 + saturation 판별 (실제 에타 스크린샷 샘플로 threshold 검증)
7. `gridSampler.ts` — bounding box + 요일수/시간범위 → occupancy 2차원 배열 생성
8. 제출 플로우 — occupancy + 이름을 submissions에 insert (이미지는 폐기)
9. `overlap.ts` + Realtime 구독 — 방의 모든 submissions 합산 → 셀별 가능 인원 카운트
10. `ResultGrid.tsx` — When2meet 스타일 히트맵 (명암 + hover)
11. 텍스트 export 기능
12. 실제 스크린샷 여러 장 + 여러 기기로 엔드투엔드 테스트, threshold 튜닝
13. Vercel + Supabase 배포