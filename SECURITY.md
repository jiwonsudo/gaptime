# 보안 정책

everyFreeTime 은 오픈소스입니다. 이 문서는 서비스의 보안 모델과 취약점 신고 방법을 설명합니다.
(운영용 상세 노트는 저장소에 포함되지 않습니다.)

## 신뢰 경계

계정이 없습니다. 접근 통제는 링크와 토큰으로만 이뤄집니다.

| 비밀값 | 부여 대상 | 저장 위치 | 권한 |
|---|---|---|---|
| 방 코드 (URL) | 방 링크를 받은 사람 | URL | 방 조회, 시간표 제출 |
| owner_token | 방 생성자 | 생성자 브라우저 localStorage | 방 관리·삭제, 제출 삭제 |
| 방장 PIN (선택) | 방장 | DB에 salt+해시로만 | 다른 기기에서 owner_token 재취득 |
| editor_token | 제출자 | 제출자 브라우저 localStorage | 자기 제출 수정·삭제 |
| 제출 PIN (선택) | 제출자 | DB에 salt+해시로만 | 다른 기기에서 editor_token 재취득 |

## 데이터베이스

- 모든 쓰기는 `SECURITY DEFINER` RPC 로만 이뤄집니다. 클라이언트는 테이블에 직접
  `INSERT/UPDATE/DELETE` 할 수 없습니다(권한 revoke + RLS).
- `rooms` / `submissions` 는 읽기만 허용됩니다(방은 만료 전). 토큰·PIN 해시가 들어있는
  테이블은 RLS 정책이 없어 클라이언트가 전혀 읽을 수 없습니다.
- 토큰·PIN 원문/해시는 클라이언트로 나가지 않습니다. RPC 내부 비교에만 쓰입니다.
- PIN 검증은 반복 실패 시 일시 잠금됩니다(무차별 대입 방어).
- RPC 는 입력값의 형식·범위·크기를 서버에서 재검증합니다(클라이언트 검증은 UX용).

## Injection / XSS

- 동적 SQL(`EXECUTE` 등)을 전혀 쓰지 않습니다. 모든 쿼리는 정적이고, 인자는 타입이
  지정된 바인드 파라미터로 전달됩니다.
- 렌더링은 전부 React(자동 이스케이프). `dangerouslySetInnerHTML` / `eval` 없음.
- 닉네임·방 이름은 서버에서 정규식·예약어·길이 검증 후 저장됩니다.

## 프라이버시

- 시간표 스크린샷 **원본 이미지는 서버로 전송되지 않습니다.** 분석은 100% 브라우저에서
  처리되고 즉시 폐기되며, 서버로는 "빈 시간/수업" 배열과 닉네임만 전송됩니다.
- 방과 제출 데이터는 생성 7일 뒤 자동 삭제됩니다.
- 사용 통계는 개인 식별 정보 없이 일별 집계(방 생성 수, 제출 수)만 보관합니다.
- 자세한 내용은 [개인정보처리방침](https://everyfreetime.cloud/privacy).

## 남용 방지

- 방 생성·제출에 IP 단위 및 전체 단위 레이트리밋이 걸려 있습니다.
- 방당 제출 수에 상한이 있습니다.
- 7일 자동 만료로 저장량이 상수 범위로 유지됩니다.

## 알려진 한계 (설계상 수용)

- **신뢰 모드 편집**: PIN 을 설정하지 않은 제출은 같은 방의 누구나 그 닉네임으로 덮어쓸 수
  있습니다. When2meet 과 같은 모델로, 방 링크가 신뢰 경계이며 방장이 잠금·삭제로 대응합니다.
  보호를 원하면 제출 시 PIN 을 설정하세요.
- **방장 토큰 이동**: 방장 PIN 을 설정하지 않으면 방을 만든 브라우저에서만 관리할 수 있습니다.
- IP 판정은 프록시 헤더 기반(best-effort)입니다.

## 취약점 신고

보안 문제를 발견하면 공개 이슈 대신 저장소 관리자에게 직접 알려주세요:
https://github.com/jiwonsudo/gaptime (Security Advisory 또는 프로필 연락처)
